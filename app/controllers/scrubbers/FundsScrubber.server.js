'use strict';

var winston = require('winston'),
	mongoose = require('mongoose'),
	Rep = mongoose.model('Rep');

/**
 * Load funds for a given representatives
 * @param {Rep} rep The rep to load funds for
 */
exports.load = function(rep) {

};